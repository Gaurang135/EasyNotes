.venv/
__pycache__/
*.pyc
data/
*.db
*.db-wal
*.db-shm
models/
.pytest_cache/

# packaged zip output (make zip)
dist/

# internal dev-process artifacts (Superpowers specs/plans) — kept locally, not pushed
docs/

# local-only helper, never commit (superseded by scripts/github-push.sh)
push_all.sh

# secrets — never commit
.env
.coverage
htmlcov/
